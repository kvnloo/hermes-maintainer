# Factory review — NousResearch/hermes-agent#108914

**Verdict: CHANGES REQUIRED.** Do not merge. Do not close #92524 or #108592 from this PR.

Exact live head at review time:

`d947fc83a461f098066c71ee6e28161736b87f53` (`d947fc8`)

If that SHA moves, this review is void. Re-pin and fail closed.

- PR: https://github.com/NousResearch/hermes-agent/pull/108914
- Author: @teknium1
- Branch: `hermes/hermes-b802e898` → `main`
- Base: `e440bf35472c30c2ee5527e884a866c12ad15b91`
- Size: +3817 / −34, 69 files, 10 commits
- `mergeable=true`, `mergeable_state=blocked` (required review / `ci-reviewed`, **not** red CI: 29 checks success, 8 skipped, 1 OSV-scanner `neutral`)
- Labels include `sweeper:risk-security-boundary`
- github_writes: origin comment posted 2026-09-13 as kvnloo after Kevin authorized [PER-1538](https://linear.app/0ism/issue/PER-1538/hitl-hermes-agent-108914-bot-screen-approve-origin-comment): https://github.com/NousResearch/hermes-agent/pull/108914#issuecomment-5649915119 (rewritten for live head `d65af42305c4`; capture/fcntl/clipboard/provenance had landed)

Ten parallel scanners plus a local sparse clone at that SHA, disk-cached GitHub REST (no further Search API after the first harvest), and independent probes that reproduced the merge-blocking bugs. Runtime, CUA, seat-policy, issue-consolidation, lockfile, test-slice, and **competing-PR** scanners independently confirmed the P1 set; they did not add a new P0. Five extra competing-PR passes (Windows/Electron, CUA seams, browser/VNC, cloud contract, leftover harvest) re-confirmed the pin and added **#104567** (draft Realms VM+wayvnc) and **#90423** (Desktop-local CUA bridge) as further product collisions — still **no clean duplicate** of Tek. A ChatGPT architecture scan on the **same SHA** independently restated P1-1/3/4/5 and added **P1-6** (stale dispatch after takeover+hand-back during approval). Their SOTA/papers tour is not merge criteria. `origin/main` has **zero** `bot_desktop` / `headed_session` / `Xvnc` / `TigerVNC` / `novnc` symbols (`DisplayTarget` on main is only unrelated `readFileDisplayTarget`). #61310 is a **semantic** clash with `auto_start` (check hides the tool; Tek starts the display only at dispatch). Test-slice: core lease/epoch/`--session` fence/1006/corrupt-lease/profile-scope/conftest pins are real (16/17 pytest node ids green after sparse `agent/` + `fastapi`/`python-dotenv`); two invariants the PR body treats as test-covered (**hero “Last seen”**, **install sudo origin routing**) have **no invariant tests**, and display-reuse coverage is **WEAK** vs the launcher `kill -0` claim. Historical “red without fix” was **not** re-run on pre-fix SHAs. Seat-policy: this is a **second-stack dump**, not composition of #97859/#17258/#90380/#108878. Lockfile: `@novnc/novnc@1.7.0` is a real MPL-2.0 Desktop dep with empty OSV for that version; the bootstrap-installer `0.0.1`→`0.21.1` field is a stale workspace rewrite (`apps/bootstrap-installer/package.json` was already `0.21.1` on base and is **not** in the PR file list). Still needs human `ci-reviewed`.

---

## What this actually is

Tek shipped the **self-hosted Linux gateway + Hermes Desktop** half of “a bot has a screen you can take over.”

That is a real product: per-profile TigerVNC `Xvnc` (Unix socket, 0600, no TCP) + Xfce under a private dbus session, a disk lease with fcntl, a server-side RFB input filter, `/api/display/ws` ticketed off the already-authenticated `/api/ws`, Desktop Take over / Hand back, install-from-Desktop via the existing sudo card, and `computer_use` refused with `code: human_has_control` while a human holds.

It is **not**:

- Nous-hosted / cloud headed sessions (PTY + WebVNC + CUA on one remote display) — that is #108592 / PER-1430
- hosted **browser** noVNC for login/CAPTCHA — that is #92524
- `DisplayTarget` / `HeadedCloudSessionProvider` from #108878
- a security boundary (docs say this; the lease does not)

The PR title still says `(#92524)`. The body correctly says Related, not Closes. Keep it that way on merge, if this ever merges.

---

## Must-fix on this SHA (P1)

Independently reproduced against the checkout at `d947fc8`.

### P1-1 — Windows `computer_use` is broken even with Bot Screen off

`handle_computer_use` always imports `tools.computer_use.handoff`, which imports `tools.bot_desktop.lease`, which does `import fcntl` at module load.

Probe: patch `__import__` so `fcntl` raises `ModuleNotFoundError`, then `handle_computer_use({'action': 'capture'})` raises `No module named 'fcntl'` before any backend.

Reported by helix4u, confirmed by MrD1az / iamlukethedev / Xipong. Still true on this head. This is a merge blocker for a cross-platform tool. `display.status` hits the same import via `_display_snapshot` before `runtime.status()` can report `supported: false`.

Gate the Linux lease behind `sys.platform` (or a portable lock) **before** the import. Add a regression that capture still works when `fcntl` is missing.

### P1-2 — Watcher can OOM `hermes serve` via ClientCutText

`RfbClientFilter._message_length` trusts the declared clipboard length. `feed()` grows `_buf` with no cap. A viewer who **never held the lease** can declare `0x7FFFFFFF` and stream body bytes forever.

Probe on this SHA: after handshake, 64 × 64KiB → `_buf` is 4,194,312 bytes, nothing forwarded, socket still open.

Reported by carlotestor, confirmed by helix4u / iamlukethedev. Cap declared clipboard length (TigerVNC’s ~1MB default is enough) and overall `_buf`; `ValueError` already maps to close 1003. **Steal, don’t reinvent:** draft **#104567** `realms/rfb.py` already uses `MAX_PAYLOAD = 1_048_576` and fail-closed unknown RFB.

### P1-3 — Local real-profile CDP browsers skip the lease fence

`_shares_bot_desktop_browser` returns false whenever `cdp_url` is set. This PR also launches local real-profile headed Chromium with the Bot Desktop env (`browser_tool_real_profile.py`). Those sessions are CDP **and** on the same DISPLAY the human is typing into.

Tek’s round-2 fence (`test_bot_desktop_browser_fence.py`) only covers agent-browser `--session` (no `cdp_url`). Julien’s original P1 is fixed for that path; the real-profile hole is still open.

Fence by **local launch provenance**, not “has cdp_url”. Keep the exemption for unrelated remote / user-attached CDP. Remote Browserbase-style CDP is not on the bot seat — that exemption is fine; the hole is local CDP on the same DISPLAY.

### P1-4 — Epoch discard happens after screenshot persist and aux-vision

`handle_computer_use` compares epoch **after** `_dispatch` returns. Capture dispatch calls `_capture_response` → `_capture_view`, which calls `_persist_capture_image` and may `_route_capture_through_aux_vision` **inside** dispatch.

Returning `human_has_control` cannot retract the image already on disk or already sent to auxiliary vision.

MrD1az F1. Check epoch before persist / vision; keep the outer fence as belt-and-braces. Existing `test_takeover_during_an_admitted_action_discards_its_result` patches `_dispatch`, so it never sees persist/aux-vision.

### P1-5 — In-flight input can still hit the seat after Take over

Epoch discard drops the **tool result**. It does not cancel an already-running click/type. helix4u: acquisition acknowledges human control without waiting for in-flight actions to stop. Source-true on this SHA. Either cancel at the backend boundary, or do not confirm “You are in control” until admitted actions can no longer affect the screen. A design note + test is the minimum if full cancel is hard.

A result that *may* have executed is not a pre-dispatch refusal. Preserve a non-sensitive uncertain-outcome receipt so the agent does not blindly replay (ChatGPT D).

### P1-6 — Takeover + hand-back during approval still dispatches the stale action

`handle_computer_use` records `admitted.epoch` at line 260, then may wait on approval / backend start. The re-check under the call lock (`tool.py:284-287`) is `assert_agent_may_act()` — **holder only**, not epoch. If a human takes over and hands back while approval is up, holder is `agent` again, the second check passes, **`_dispatch` runs the old click**, and only the *postflight* epoch compare (`tool.py:291`) discards the result.

```
Agent admitted at epoch 0
Approval waits
Human acquire → epoch 1, then release → epoch 2
assert_agent_may_act() sees AGENT → dispatch executes
postflight notices epoch 2 ≠ 0 → human_has_control
```

Existing `test_takeover_during_an_admitted_action_discards_its_result` does acquire+release **inside mocked `_dispatch`**, so it never proves “no old-generation dispatch.” Compare `admitted.epoch` **immediately before** `_dispatch` (and still after). ChatGPT B; confirmed on this pin, not an excerpt-only claim.

---

## P2 still open (fix with the P1s; do not “ship it”)

| ID | Bytes | Status |
| --- | --- | --- |
| Portal listener | `screen-portal.tsx` still matches `payload.profile_key` only. Pane/install use `isEventForBotScreen`. Tek claimed this fixed in `d947fc8`; the **portal** consumer was not converted (the file Julien commented on). Tests cover the predicate, not `useScreenPortalState`. Hero **freshness** is fixed; **who** holds can still lie via this listener. | Residual of Julien P2 |
| Pane close ≠ hand back | `detach()` / noVNC 1.7.0 `WebSocket.close()` with no status → server **1005**. Bridge only releases 1000/1001. Docs, pane comment, and `bot-screen.md:68` say closing the pane hands control back — that is the documented handback path, now inverted by the 1006 fix. Do **not** unconditionally `release` inside `detach()`: `attach()` calls `detach()` first. | helix4u / MrD1az / Xipong; UI scanner rates this P1 on contract |
| `4000 control-taken` | File header: eviction “simply re-attaches in watch mode.” Auto-attach only runs when `conn === 'idle'` — pane stays on a dead stream until refresh. Eviction callback is **in-process only** (`lease.on_change`); a cross-process `acquire` still blocks RFB input via the file, but may not close the old socket with 4000. No test hits 4000. Filter vs file is unlocked per message (at most one RFB event after a flip). | UI + lease/WS scanners |
| Hardcoded EN | `screen-open.tsx`: `'Update Hermes Desktop to open bot screens.'` and `` `${…} · Screen` ``. Rest of screen copy is localized (en/ja/zh/zh-Hant). | UI scanner |
| CLI `screen stop` | `_screen_stop` calls `runtime.stop()` only. Gateway `display.stop` releases. Help text says stop hands control back. Probe: lease stays human. | MrD1az F7 |
| Display alloc race | `_allocate_display` releases the host flock before Xvnc creates `/tmp/.X{N}-lock`. Two cold starts can both pick `:20`. Restart-steal of a *live* `:N` is already fixed (`_display_in_use` + launcher only unlinks a dead lock). | MrD1az / Xipong; runtime scanner |
| Thumbnail `XAUTHORITY` | Process-wide `os.environ` swap; gateway worker threads can cross-contaminate profiles. | MrD1az F8 |
| Lease shapes | `{}` and `{"holder":"invalid"}` admit the agent. `[]` raises `AttributeError` (not in the corrupt-file except). Fail closed on unknown shapes. | Probe on this SHA |
| RFB socket audience | `launcher.sh` says `SecurityTypes None` is safe because “the 0600 socket is reachable solely by the gateway.” False: any **same-UID** process that can see `<HERMES_HOME>/bot-desktop/rfb.sock` can dial it (`-AlwaysShared`). Other UIDs cannot. Docs should match Julien: not a security boundary. | Runtime scanner |
| `zypper` in PR body | CLI blurb lists apt/dnf/pacman/**zypper**. `PACKAGES` / `install_command()` are apt/dnf/pacman only. Docs table is honest; PR body is not. | Runtime scanner |
| `ci-reviewed` | `@novnc/novnc@1.7.0` is a real `apps/desktop` dep (MPL-2.0, integrity matches registry, no OSV hit on 1.7.0). Bootstrap `0.0.1`→`0.21.1` is a lockfile workspace-version refresh, not a sneaky package bump — still unexplained in the PR and still a merge label. | CI bot + lockfile scanner |
| Docs vs default | `bot-screen.md` YAML shows `auto_start: true`; `computer-use.md` says Hermes “starts on first use.” Code default is **off**. Intro lists “Hermes Cloud” as a host; pane “works over Hermes Cloud connections” is Desktop→gateway transport, not #92524. Binding “never acts on a display a human is sitting at” is only true **while** Bot Desktop is running (`desktop_env` is a no-op otherwise). | Seat-policy scanner |
| `display.*` authz | `@_profile_scoped` only swaps `HERMES_HOME`. Any authenticated gateway JSON-RPC peer can start/install/acquire. Fine for single-user self-host; weak on a shared gateway. | Lockfile/authz scanner |
| Julien inline anchors | Two round-2 P1 threads (`tool.py`, `display.py`) still attach to `cc0814d` with `line=null` after force-push. Epoch discard + 1000/1001 release **are** on this head. Do not treat GitHub “outdated” as unfixed. Portal/browser-tool threads remapped. | Timeline scanner |
| Shared OS user | Julien’s design note, still true. Terminal / files / child processes are not fenced. `dbus-run-session` is a private bus, not a UID jail. Docs already say “work surfaces, not security boundaries.” Do not overclaim in the merge commit. | Design, not a silent P1 |
| Shared-desktop writer lock | `_backend_call_locks` is keyed by `session_id`. The profile lease names a generic agent holder. Two sessions on one Bot Desktop can both pass `assert_agent_may_act` and interleave clicks. Not proof of simultaneous native events; proof the wrapper has no resource-scoped writer exclusion. | ChatGPT E; P2 unless two-session CU is in-scope |

---

## Comment ledger (every thread on this PR)

| Who | On SHA | Treat as |
| --- | --- | --- |
| github-actions ci-review | `d947fc8` | Action required: lockfile. OSV list is mostly pre-existing pins, not a new 76-CVE dump from this PR. |
| @teknium1 (issue comment + inline replies) | claims round-2 fixed in `7a43ce8` / `d947fc8` | Round-2 **browser `--session` fence, epoch-only discard, 1006 keeps lease, hero Last seen (code), corrupt-file fail-closed, pending_handoff as reason** are in the tree. PR “vitest” for hero stale is **false** (zero vitest refs). Sudo origin routing is in code, **untested**. Portal “fixed” is **incomplete**. Windows / RFB cap / real-profile CDP / persist-before-epoch / 1005 were not in that round. |
| @frosty00 | `d947fc8` | Drive-by (“my agent checked it”). No bytes. Ignore. |
| @helix4u | `d947fc8` | **KEEP this comment.** Windows P1, RFB buffer, in-flight overlap. All still open. |
| @MrD1az | `d947fc8` | Best independent review on the head. F1–F5 must-fix / P2 are real; probes are included. F6–F8 should-fix are real. |
| @Julientalbot | `cc0814d` (older) | Round-2 P1s were the right control contract. Tek addressed the five probes on the paths they hit. Residual: portal consumer + OS-account confidentiality. Do not re-litigate the fixed probes; do not treat the round as closed. |
| @carlotestor | `d947fc8` | Strong RFB adversarial pass **plus** the OOM finding. Not a rubber stamp. |
| @iowahawkeyedave | `d947fc8` | 16 probes on lease/RFB are useful and passed. **“Ship it” is wrong** — missed Windows, OOM, CDP, 1005. Treat as partial KEEP on the lease/RFB core, not a merge vote. |
| @iamlukethedev | `d947fc8` | Request-changes. Agrees the P1 set. Correct. |
| @Xipong | `d947fc8` | Windows P1 + 1005 + alloc + portal residual. Correct. |
| Tek empty review shells | `d947fc8` | Inline replies only. Not an approval. |

---

## Tests vs claimed invariants

Pytest at pin `d947fc8` (sparse clone + `agent/` + `fastapi`/`python-dotenv`): **16/17 node ids green** for the Python invariants below. Desktop vitest was read statically, not executed here. Historical “red without the fix” was **not** re-run on pre-fix SHAs — only statically inferred where the assertion encodes the bug.

| Claim | Test | Verdict |
| --- | --- | --- |
| RFB input gated, chunked, QEMU 255 | `test_bot_desktop_lease.py::test_rfb_filter_forwards_input_only_from_the_lease_holder_across_arbitrary_chunking` | VERIFIED. No size cap. |
| Takeover+hand-back during approval must not `_dispatch` | none that compare epoch *before* dispatch | **MISSING.** Postflight test patches `_dispatch`. ChatGPT B / P1-6. |
| Cross-process lease (file+fcntl, not per-process dict) | `test_lease_authority_is_shared_across_processes` | **VERIFIED**. Real subprocess + shared `HERMES_HOME`; child sees `HUMAN`. |
| Corrupt / unreadable lease → HUMAN (fail closed) | `test_unreadable_lease_file_fails_closed_and_takeover_keeps_the_agents_reason` | **VERIFIED** for missing file → AGENT and `"{ torn"` → HUMAN. Weak for `{}` / invalid holder / `[]`. |
| `pending_handoff` kept as `reason` on acquire | same function (second half) | **VERIFIED**. `acquire` without `reason=` keeps handoff text on `reason`. |
| Browser `--session` fenced while human holds | `test_bot_desktop_browser_fence.py::test_browser_click_is_fenced_while_human_controls_shared_browser` | **VERIFIED**. Hits real fence before mocked `_spawn_and_collect`; `commands==[]`, `code=human_has_control`. |
| Browser result across takeover→hand-back discarded | `…::test_browser_result_crossing_a_takeover_is_discarded` | **VERIFIED**. Same epoch pattern as computer_use. |
| `browser_snapshot` under lease | none | **MISSING**. Only `browser_click` is covered. |
| Cloud / user-attached CDP negative (fence must not engage) | none | **MISSING**. `_shares_bot_desktop_browser` skips `cdp_url` but no test proves that exemption; local real-profile CDP hole is P1-3. |
| 1006 keeps lease, 1000 releases | `tests/hermes_cli/test_display_ws_drop_keeps_lease.py` (`[1006-True]`, `[1000-False]`) | **VERIFIED** against real `display._bridge` + fake WS. Code: `_CLEAN_CLOSE={1000,1001}`. **MISSING 1005** (pane `close()`), **MISSING 1001** (minor; 1001 is in the set). Stale `.lock` file after crash is not a sticky flock (kernel unlocks). |
| Display ticket | `test_display_ws_ticket.py` | VERIFIED wrong provider / unpinned / reuse. **WEAK** on expiry: docstring claims TTL, body never advances time. Ticket consumed **before** RFB dial (`display.py:55-81`); observe-then-stop wastes the one-shot. |
| Install cancel / busy | `test_bot_desktop_install.py` | VERIFIED. |
| Install worker keeps named-profile `HERMES_HOME` | `tests/tui_gateway/test_display_methods.py::test_install_worker_keeps_the_requested_profile_scope` | **VERIFIED**. Asserts `get_hermes_home()` inside worker == named profile. Implementation uses `copy_context()`. |
| Install sudo reply must not ride foreground `$gateway` | none | **MISSING**. Code pins `SudoRequest.origin` + `requestGatewayForAgent` (`input-requests.ts` / `prompt-overlays.tsx`). “tsc + vitest green” ≠ this invariant. |
| Dock Browser ≡ agent browser identity | `tests/tools/test_bot_desktop_browser.py::test_dock_and_agent_share_browser_identity` | **VERIFIED** same exe + `browser-profile` path. Live localStorage proof is outside the suite (WEAK as E2E, VERIFIED as unit identity). |
| Restart must not reuse a live display / unlink live X lock | `test_bot_desktop_runtime.py::test_recorded_display_held_by_a_live_server_is_not_reused` | **WEAK**. Only mocks `_display_in_use` around `_allocate_display()`. Does **not** exercise `launcher.sh` live-pid guard (`kill -0` before `rm` of `.X*-lock`). |
| Hero “Live” → “Last seen” after 3 thumbnail misses | none | **MISSING**. Logic in `screen-hero.tsx` (`STALE_AFTER=3`) + 4-locale `heroStale` strings. **Zero vitest references.** PR “vitest” claim is false for this invariant. |
| Events keyed by connectionId **and** profile_key | `apps/desktop/src/plugins/hermes-bots/screen-connection.test.ts` (`isEventForBotScreen`) | **VERIFIED** predicate. **WEAK** wiring: three call sites in pane/install use it (code read); no test locks the call sites. Portal listener still **not** converted. |
| Windows import | none | MISSING. |
| RFB buffer cap | none | MISSING. |
| CLI stop releases | none | MISSING (and currently false). |
| `4000 control-taken` | none | MISSING. |

`tests/tools/conftest.py` autouse `_no_host_bot_desktop_autostart` patches `runtime.missing_binaries` → `["Xvnc"]` so `_should_auto_start` is false. **VERIFIED** — the right class of fixture; host TigerVNC will not auto-start under this suite.

KEEP review would still demand: (1) hero stale unit/hook test, (2) install sudo origin routing, (3) launcher `kill -0` live-lock non-unlink, (4) `browser_snapshot` under lease + CDP/cloud negative, (5) optional: parametrize clean close 1001; contract that all three UI listeners call `isEventForBotScreen`.

---

## Related issues — consolidate vs keep split

| Issue | Ask | 108914 | Action |
| --- | --- | --- | --- |
| **#92524** | Hosted/cloud **browser** noVNC, human login/CAPTCHA, dashboard | Local Linux desktop + shared Chromium profile. Hosted browser + dashboard **not** here. | **Related, do not close.** Title `#92524` must not auto-close. |
| **#108592** | Headed **cloud** sessions: PTY + WebVNC + CUA on one remote display | Self-hosted Linux Xfce. No PTY surface, no cloud provider, no `DisplayTarget`. | **Distinct. Do not close.** Linear PER-1430 stays on 108878 + a future provider, not this PR. |
| **#107480** / PER-1320 | Headless Cursor Cloud Agents skill | Unrelated. | Keep split. |
| **#90435** | Unify Desktop Preview + Browser into a Workspace Viewer | Screen is a third surface (Xfce), not a unified preview. | Related UX, keep open. |
| **#83073** | Visible browser panel in Desktop | Browser-only. This PR is a full desktop. | Distinct. |
| **#84000** | Chrome extension shared-control | Distinct backend. | Distinct. |
| **#15876** | Optional desktop CUA + noVNC (closed) | Ancestor of #17258. | Already closed. |
| **#103932** / **#71157** | Remote computer_use on another machine | Different: drive cua-driver over the network, not a local Xfce. | Distinct. Compose later with #104080. |
| **#100428** / **#94827** | Headed browser / missing X | This PR can *supply* a DISPLAY on Linux. Does not implement per-session headed-mode or `.env` inheritance. | Related; keep #100468 / #94838. |
| **#102662** | Windows `browser_exec` headed | Linux-only screen. | Already duplicate of #100428. Distinct from 108914. |
| **#100826** | First-party rentable always-on Linux VM | DIY VPS + this PR is a subset. No billing/Portal SKU. | Distinct. Optional Related only. |
| **#69951** / **#48760** | In-app Desktop browser pane | Bot Screen is a desktop, not an embedded Browser Use panel. | Distinct. Keep with #83073/#90435. |
| **#47749** | Camofox plugin VNC | Different stack. | Distinct. |
| **#67653** | Cloud browser session persistence | Cloud Browserbase. | Distinct. |

**#92524 remaining slices after 108914 (leave the issue open):** A = this PR (self-hosted Linux + Desktop). B = hosted/Portal cloud-**browser** dashboard Watch/Take over. C = headed cloud **session** (#108592 / #108878). D = user’s real Chrome (#84000). E = in-app Viewer/browser pane (#90435/#69951).

False friends discarded: session-lease / kanban-lease / Bot Mode replica / filter-UI / “display”=label PRs. “Lease” in this repo is overloaded.

---

## Related PRs — compose, don’t duplicate, don’t merge two desktops

File overlap vs 108914: **#108878 / #90374 / #97772 / #108660 = 0 files.** Soft (mergeable, mostly non-overlapping hunks) on CU/browser/config/docs: #90380, #61310, #104080/#103653, #100468, #9910, #106438, #90423, #104567, #108832, #77672, #94838. Hard **product** collisions (not just git): **#97859**, **#17258**, **#104567** (third Linux-desktop-in-Desktop: Omarchy VM + wayvnc from a plugin), **#90423** (third “whose keyboard”: human Desktop is the seat).

Five extra competing-PR passes (A–E) at this pin: no open PR newer than 108914 is a second TigerVNC+Xfce+lease stack. `DisplayTarget` still exists only on **#108878**. No competing body uses `Closes #92524` / `Closes #108592`; the only auto-close hazard remains 108914’s title `(#92524)`. Open-PR list REST caps at ~5000–8000 newest; older mapped PRs were fetched by number.

| PR | What it is | vs 108914 |
| --- | --- | --- |
| **#108878** kvnloo `1b723a92` | RFC + `agent.headed_session.DisplayTarget` / `HeadedCloudSessionProvider` / `bound_display_fingerprint()` / fail-closed #90374. **No Xvnc.** | **KEEP-as-complement.** 0 file overlap. Tek does **not** import these types (confirmed `NONE` on pin). For **108914 as shipped: do not import them** (self-hosted Bot Screen ≠ headed cloud session). For **#108592 later: yes** — wrap Tek’s DISPLAY as a `DisplayTarget`; do not mint a second contract. Steal types + `assert_computer_use_targets_session` / `bound_display_fingerprint`, not another X11 stack. |
| **#97859** Sunwood +1948 | Profile-scoped Bot Desktop (WSL2/Docker Xvfb + Electron pane) | **8 soft files** (`package.json`, i18n, `bot-row.tsx`, `sdk/index.ts`). Core is disjoint (`electron/bot-desktop-runtime*.ts`, `bot-desktop-pane.tsx`, Dockerfile). **Not a clean supersede:** Windows/Desktop-spawned Linux vs Tek’s Linux-gateway native. **Product duplicate risk:** two “Open Bot Desktop/Screen” surfaces. Align naming/menu; do not copy the Electron runtime into Tek’s Xfce path. Close-as-superseded only if maintainers pick Linux-gateway as the only story. |
| **#17258** 0xMrBlueOps +731 | Optional Docker Xfce + noVNC for computer_use | **3 soft:** `config_defaults.py`, `cua_backend.py`, `computer-use.md`. New tree: `hermes-desktop/*`. **KEEP-as-complement** (container path). Provenance for launcher shape Tek reinvented in `launcher.sh`. If maintainers want one default story, Tek **supersedes** for first-party gateway desktop; keep 17258 as the optional image (container isolation is the confidentiality 108914 punted). |
| **#104567** Zeus-Deus (draft) | Bundled `hermes-realms`: profile-scoped **Omarchy VM + wayvnc** + vendored noVNC, opened from a Desktop plugin. RFB `ClientFilter` in `realms/rfb.py`. | **NEW (pass A).** **10 soft files** (i18n/sdk/CU/gateway/`pyproject.toml`). Core tree disjoint (`plugins/hermes-realms/**`). **Third** “Open a Linux desktop in Desktop” surface, spawned from Electron, not gateway `tools/bot_desktop`. **KEEP-as-complement** as VM isolation. Align naming. **Steal** `MAX_PAYLOAD = 1_048_576` for P1-2. Do not copy QEMU/wayvnc into Tek’s Xfce path. Precursor **#103690** is isolated plugin viewers only — DISTINCT. |
| **#90423** OutThisLife | Hermes Desktop drives **your** machine while the agent runs elsewhere (`desktop-bridge` / `http-bridge`; Desktop wins `session_computer_use_provider()`). | **NEW (passes B/C).** **~10 soft files** including `tool.py`. Depends on 90380-shaped registry. **Third whose-keyboard story** vs Bot Screen lease vs #97859. Inverse topology of 108914. Do not dump both as the default Desktop CUA path. |
| **#90380** OutThisLife | Pluggable CUA backends | **3 soft.** Tek patches dispatch/lease (`tool.py` ~251–295); 90380 rewires provider resolve / `check_*`. Adjacent, mergeable. Prefer composing Bot Desktop as a registered display provider later over a second backend cache. |
| **#90374** OutThisLife | Don’t act on the wrong machine/seat | **0 files.** Locality stamp + HUD. Orthogonal to Tek’s human↔agent **lease** on one display. 108914 `desktop_env()` injects the bot DISPLAY **when the screen is running**; headed Linux with the screen **off** still uses process `DISPLAY`. Keep. |
| **#61310** jarrelscy | Require a Linux `DISPLAY` before exposing computer_use | **1 file, semantic clash not hunk clash.** 61310 edits `check_computer_use_requirements`; Tek edits `handle_computer_use` + `ensure_started_for_tool`. If 61310 merges first, the tool stays **hidden** on headless until DISPLAY exists — Tek’s `auto_start` only runs **at dispatch**, after the tool is already exposed. Tek must (a) publish Bot Desktop DISPLAY before `check_*`, or (b) document auto_start + 61310 ordering. |
| **#97772** Dejmacro | Camofox external noVNC discovery | **0 files.** `/vnc/status` + `browser.camofox.vnc_url`. Browser viewer URL ≠ Bot Screen RFB. |
| **#9910** jbentley-agent (draft) | Camofox takeover tool | **1 soft:** `tools/browser_tool.py`. Browser-session takeover ≠ Bot Screen lease/RFB filter. |
| **#104080** / **#103653** bennybuoy | Remote CUA transport (drive another machine) | **5 soft** on CU core. Different topology from spawn-local-Xfce. Prefer **one** of 104080/103653; 103653 already includes a repaired provider seam + remote — mutually exclusive with stacking **both** 104080 and full 90380. Sequence vs 108914, don’t ignore the merge. |
| **#100468** 0xble | Headed mode per `browser_exec` session | **1 soft:** `browser_tool_real_profile.py` (Tek: tiny `desktop_env` wiring; 100468: session headed bind). Browser visibility ≠ desktop. |
| **#108660** TotalLag (draft) | VPN take over the agent’s **browser** | **0 files** (optional-skill tree). Steal ops runbook only (Xvfb→x11vnc→websockify loopback). Do not confuse with screen takeover. |
| **#106438** teknium1 | Sensitive-app blocklist | **3 soft:** `config_defaults.py`, `tool.py`, docs — different hunks than the lease. Should land independently; merge both carefully. More important on a shared OS user desktop. Do not merge 108914 as if that blocklist exists. |
| **#107031** KoNit-K | Desktop session-claim vs sibling leases | Different lease (session ownership, not screen). Naming collision only. |
| **#102113** bennahub | AgentComputer + BrowserIdentity + **ControlLease** / `fencing_epoch` on **same-host Chromium**. DESIGN.md: do **not** build VNC. | **NEW (passes A/C).** 3 soft files. Second **lease** vocabulary, screenshot-on-demand live view. **DISTINCT** (explicitly anti-VNC). Compose control-theory later; not a Bot Screen. `mergeable_state=dirty`. |
| **#62812** leomercer-dev | Per-thread Camofox **Xvfb + VNC + noVNC** viewer | **NEW (passes A/C/E).** Soft `browser_tool.py` only. Looks like a screen; is a **browser-session** pool. Same cluster as #97772/#9910. Do **not** close #92524. |
| **#94838** Finn763 | Headed Chromium + no DISPLAY → error + **Xvfb** (Closes #94827) | **NEW on the PR map** (issue #94827 was already named). 1 soft: `browser_tool.py`. If Bot Desktop published DISPLAY, this path should stay idle; if screen is **off**, Finn starts private Xvfb **without** Tek’s RFB/lease. |
| **#108832** liuhao1024 | Fail closed when `computer_use` dispatch races backend release | **NEW (passes B/E).** Inserts `_detached_backends` under the same call lock Tek uses for `assert_agent_may_act` + epoch discard (`tool.py:278-295`). Adjacent, mergeable; three-way pain if stacked with 104080. Session-cache, not bot lease. |
| **#77672** handnewb | Auto-start **cua-driver daemon** on Linux | **NEW (passes B/E).** 1 file: `cua_backend.py`. Pin already injects `desktop_env` into `cua_driver_child_env`. Two different Linux auto-start knobs (driver daemon vs Xfce). #61310 can still hide the tool. |
| **#100810** MrTheSoulz | MCP cua-driver `--no-overlay` + interrupt teardown (Closes **#81220**) | **KEEP compose (pass E).** Overlay would steal pointer on Tek’s Xfce. 0 overlap with 108914 filenames (`cua_backend_driver.py`). Not a desktop dump. Pass D DISPROVE as headed-session contract. |

P1-3 (local real-profile CDP skips fence) is **not** closed by **#98319** (hide window), **#109347** (hard same file `browser_tool_real_profile.py`, HTTP `with` only), **#103069** (which Chromium), or **#108603/#108600** (loopback CDP classified local). Hiding or picking a profile does not fence agent CDP on Bot DISPLAY.

**False friends (do not add):** #108879 (VOL kit), #101563 (ffmpeg screen *video*), #60375 (operator Desktop GPU-off on TigerVNC), closed #50622 (`hermes novnc` CLI — do not revive), #103931 (closed twin of #103932), session/kanban/turn-lease cluster, Hermes Desktop chrome, Bot Mode / replica takeover #99107, Camofox docs #97391/#97185/#97452/#83497/#109066 (exec, not viewer).

Compose later, don’t dump: (1) #108878 types for cloud, (2) #97859/#17258/#104567 launcher/UX/isolation ideas — don’t ship **four** desktops, (3) **one** of #90380 or #103653 as the provider seam; do not also dump #90423 as default, (4) #90374 + #61310 + #106438 + #100810 as orthogonal safety, (5) browser-takeover cluster (#97772/#9910/#108660/#100468/#62812/#102113) stays complementary.

Policy vs PER-1430: **108914 dumps a second desktop stack** (TigerVNC + Xfce in-tree). That violates the “compose, do not dump” rule for *cloud* headed sessions. For *self-hosted Linux bots* it is the feature. Honest merge language: “Linux Bot Screen,” not “headed cloud sessions.”

---

## What is genuinely good (so the P1s don’t bury it)

- On-disk lease + epoch is the right architecture for serve / gateway / CLI not sharing memory. Cross-process test is real.
- Server-side RFB filter (not noVNC `viewOnly`) is the load-bearing input gate. carlotestor’s extra probes show framing holds; the hole is size, not type confusion.
- 1006 / abort keeping HUMAN is the correct control-theory after Julien. The new 1005 bug is the cost of that fix plus noVNC’s `close()`.
- Ticketed `/api/display/ws` off authenticated `/api/ws`, Unix socket 0600, no TCP, `-SecurityTypes None` only behind the gateway — coherent for a single-user gateway. Not coherent as a multi-user security boundary (and docs say so).
- Sudo password on the requesting connection, empty cancel, one install per profile — that round-1 P1 looks actually fixed.
- Scope honesty in the body (Related not Closes, auto_start off, request_handoff does not send Telegram) is rare and correct.
- Live Xvnc/Xfce evidence in the body is more than most PRs of this size.

This is a serious piece of product engineering. It is not merge-ready on `d947fc8`.

---

## ChatGPT architecture scan vs this review (same pin)

Source: [chatgpt.com/share/6aa5d156-…](https://chatgpt.com/share/6aa5d156-b41c-83e9-9bee-b97de02e4ded) titled “Find SOTA Architecture.” Same SHA `d947fc8`. Their method: reconstructed excerpts + inert device/vision stubs, **not** a full Hermes checkout. Ours: sparse clone, live probes, GitHub comment ledger, Desktop/RFB/tests, related-PR map.

Treat their A–E as **control-boundary** findings. Do not treat the SOTA/papers/harness tour as merge criteria for this PR.

| Their finding | Ours | Synthesis |
| --- | --- | --- |
| **A** Capture persist / aux-vision before outer epoch refuse | **P1-4** (MrD1az F1). They lead with this; so should a maintainer pitch. | Same bug. Adopt their test shape: assert **no** persist/vision call, not merely `human_has_control` on the return. |
| **B** Takeover + hand-back during approval; second check is holder-only; stale `_dispatch` still runs | **Missing until this fold → P1-6.** Confirmed on pin: `assert_agent_may_act()` at `tool.py:284-287` does not compare `admitted.epoch`. Our existing epoch test patches `_dispatch`, so it never forbids the click. | **Promote.** Smallest patch: epoch compare immediately before dispatch. |
| **C** `fcntl` via `handoff` → `lease` on every CU action, including Windows | **P1-1.** They simulated `ModuleNotFoundError` on Linux; we did too. They correctly say native Windows still needs its own regression. | Same. Keep “do not broadly `except ImportError` and silently drop the fence.” |
| **D** Takeover RPC returns HUMAN before admitted input has stopped | **P1-5** (helix4u). They add: an executed-or-not result must not look like a pre-dispatch refusal. | Same hole + **uncertain-outcome receipt**. Takeover is a **barrier** (QUIESCING), not a label. |
| **E1** Session call-lock ≠ desktop writer lock | **New P2.** | Real; not a merge blocker unless two CU sessions on one profile are in-scope. One writer per shared desktop. |
| **E2** `_shares_bot_desktop_browser` false on any `cdp_url` | **P1-3.** They call it a configuration counterexample; we reproduced the **default** local real-profile path (`browser_tool_real_profile.py` + `_build_browser_env`). | Same hole, we are stricter: it is not merely a misconfig, it is this PR’s own headed Chromium. Fence by launch provenance. |
| “Desktop reference / control permit / sealed observation” | Matches **#108878** `DisplayTarget` + `bound_display_fingerprint` + this PR’s disk lease/epoch — if composed later. | **Do not mint a third contract in 108914.** Bind Tek’s DISPLAY as a `DisplayTarget` on the #108592 follow-up. Session owns backend lifetime; the **desktop** owns control. That is already the 108878/90380/#103653 direction. |
| QUIESCING state machine | Stronger statement of P1-5/P1-6. | Design note for Tek; not a reason to rewrite the PR around cua-driver. |
| SOTA / OSWorld / bounded action blocks / training | Out of scope for merging Bot Screen. | Performance experiments only **after** 1–5b. A faster driver cannot recover a revoked capture that already hit vision. |
| Issue **#47072**, PR **#108460**, **#103653**, **#103690** as existing architecture | #103653/#103690 were already on our map. **#47072** (thin cua-driver wrapper roadmap) and **#108460** (element-token schema) were not. | Compose later. Do not pretend 108914 is that wrapper. |

**What they missed (keep ours):** RFB ClientCutText OOM (P1-2); portal listener; pane close → 1005; `4000` re-attach; CLI `screen stop`; display alloc race; test-coverage honesty (hero/sudo/launcher); title `(#92524)` auto-close; competing desktops **#97859 / #17258 / #104567 / #90423**; `ci-reviewed`; docs vs `auto_start`.

**What we missed (now folded):** P1-6; uncertain receipt; shared-desktop writer lock; their test table (no old-generation dispatch; no revoked frame to persist/vision; human-already-holds must not start a device op).

**How to talk to Tek:** one control boundary, two patches this PR can take without a framework: (1) isolate `fcntl`, (2) epoch-before-dispatch + epoch-before-persist/vision. The “architectural unlock” they want is **resource identity**, which #108878 already named. 108914 should remain Linux Bot Screen with a correct lease, not a new orchestration layer.

---

## Merge checklist (for Tek, not for us to implement unless asked)

1. Gate `fcntl` / lease so Windows `computer_use` still works; regression with `fcntl` missing.
2. Cap RFB ClientCutText / `_buf`; watcher OOM test. (Mirror #104567 `MAX_PAYLOAD = 1_048_576`.)
3. Fence local real-profile CDP on the same DISPLAY.
4. Epoch check before persist + aux-vision.
5. In-flight input: cancel or don’t ack takeover until the seat is quiet. Uncertain-outcome receipt if the action may already have run.
5b. Compare `admitted.epoch` **immediately before** `_dispatch`, not only after. Takeover+hand-back during approval must not run the old action (P1-6).
6. `isEventForBotScreen` in the **portal** listener; test the hook with two connectionIds.
7. Intentional pane close → `close(1000)` **or** `lease.release` on unmount only (not on `attach()`’s `detach()`); 1006 still holds. Align docs. After `4000 control-taken`, actually re-attach in watch mode. Broadcast 4000 across processes (or poll the file in the bridge), not only `on_change` in-process.
8. CLI `screen stop` releases, matching `display.stop`.
9. Hold display alloc until Xvnc claims, or pending reservation.
10. Fail-closed lease shapes; isolate thumbnail env; localize `screen-open.tsx`.
11. Human `ci-reviewed` after confirming bootstrap-installer `0.21.1` is a stale workspace-version rewrite (already true on base `package.json`).
12. Docs: YAML example `auto_start: false` (or mark as opt-in); fix `computer-use.md` “starts on first use”; stop implying Hermes Cloud *is* Bot Screen. If **#61310** lands first, publish Bot Desktop DISPLAY before `check_computer_use_requirements`, or document that `auto_start` cannot un-hide a tool that check already stripped.
13. Tests the PR body already claims: hero 3-miss “Last seen”; sudo origin `connectionId`/`profile` (not foreground `$gateway`); `launcher.sh` `kill -0` before unlinking `.X*-lock`; `browser_snapshot` under lease; CDP/cloud negative (fence must not engage).
14. Title/body trailer (never `Closes`):

```
Related: #92524 (self-hosted Linux+Desktop half only; hosted/dashboard cloud-browser remains),
#108592 (headed cloud sessions — not this), #15876 (closed ancestor), #17258, #97859,
#104567 (Realms VM/wayvnc — different stack), #90423 (Desktop-local CUA bridge),
#90380, #90374.

Does not close: #92524, #108592, #107480, #90435, #83073, #84000, #69951,
#100428, #103932, #94827, #71157, #100826, #47749.
```

Until 1–5b land, **CHANGES REQUIRED**. Item 13 is honesty about claimed coverage, not a new product P0.

No origin comment, no merge, no competing PR from this review unless Kevin asks to post.

---

## Origin comment (posted)

Posted: https://github.com/NousResearch/hermes-agent/pull/108914#issuecomment-5649915119

Bytes in `HELPING-TEK.md` (rewritten for `d65af42`: remaining input-dispatch hole only).

---
