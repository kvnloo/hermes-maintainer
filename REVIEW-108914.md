# Factory review — NousResearch/hermes-agent#108914

**Verdict: KEEP.** No remaining P1 on this SHA. Do not close #92524 or #108592. Do not post another origin comment unless asked.

Exact live head at this review:

`bc36ddb5f9696c25acc5d51cf29a961710e2d5a3` (`bc36ddb5`)

If that SHA moves, this review is void. Re-pin and fail closed.

Prior pins **VOID:** `d947fc83` (first factory), `d65af423` (third-round / kvnloo comment).

- PR: https://github.com/NousResearch/hermes-agent/pull/108914
- Author: @teknium1
- Branch: `NousResearch:hermes/hermes-b802e898` → `main`
- Base: `205645ee424163c7b6cfc032c331c3557797497b` (rebased onto multiplex-profile main)
- Size: +6295 / −52, 93 files, 58 commits
- `mergeable=true`, `mergeable_state=blocked` (human `ci-reviewed`, not red CI). Python tests / e2e / JS+TS / footguns **success**; Windows-only + nix were still in_progress at fetch; OSV `neutral`; Desktop E2E skipped
- github_writes: kvnloo origin comment on VOID `d65af42`: https://github.com/NousResearch/hermes-agent/pull/108914#issuecomment-5649915119. Leftovers (title / auto_start YAML / close dirty follow-ups) posted on this SHA: https://github.com/NousResearch/hermes-agent/pull/108914#issuecomment-5650142924

Fourth round vs `d65af42`: cherry-pick **#109508** (`_fence()` immediately before `_dispatch` + recording-backend test), cherry-pick **#109505** (native Windows/macOS registry dispatch tests), watcher also stats `env` + `launcher.pid` and broadcasts `display.status`, xkbcomp noise filter, ignored-release log, dock `--test-type`. Those three follow-up PRs are still **open + dirty** against this HEAD (cherry-picked, not merged as PRs). Close them after 108914 lands.

Independent probe on this SHA: `test_takeover_handback_during_approval_does_not_start_the_device_op` **green**. Approval wait → acquire+release → `backend.calls == []` and `code == human_has_control`. That was the last merge-blocking control bug.

---

## What this actually is

Self-hosted Linux gateway + Hermes Desktop: per-profile TigerVNC `Xvnc` (Unix socket 0600, no TCP) + Xfce, on-disk lease + epoch, server-side RFB filter, ticketed `/api/display/ws`, Take over / Hand back, `computer_use` / local browser fenced with `human_has_control`.

Not hosted cloud-browser (#92524), not headed cloud sessions (#108592 / PER-1430 / #108878 `DisplayTarget`), not a security boundary.

Title still ends `(#92524)`. Body Related / “does not close” is still correct. zypper is gone from the body (Tek: jeremiah-k #25).

---

## ChatGPT posts vs this SHA

Two shares. WebFetch of the live pages is a logged-out JS dump; the substance is the conversations we already extracted, re-read against `bc36ddb5`.

### [Find SOTA Architecture](https://chatgpt.com/share/6aa5d156-b41c-83e9-9bee-b97de02e4ded) (`6aa5d156`)

Originally reconstructed against **VOID** `d947fc8`. Control-boundary findings only. SOTA / OSWorld / papers are **not** merge criteria.

| Their finding | At `bc36ddb5` |
| --- | --- |
| **A** Capture persist / aux-vision before epoch refuse | **LANDED** (round 3). `fence()` after `backend.capture()` before `_capture_response`. |
| **B** Approval wait → holder-only recheck → stale click still runs | **LANDED** (round 4 / #109508). `_fence()` at `tool.py:303` before `_dispatch`. Input still does not *receive* `fence=` (delivery kwargs); the pre-dispatch compare is the missing use. |
| **C** `fcntl` via handoff → lease on every CU call | **LANDED** (round 3). Optional import + footgun checker. |
| **D** Takeover RPC returns HUMAN before in-flight input stops | **By-design skip** (Tek, unchanged). Uncertain-outcome receipt remains a good follow-up, not a P1. |
| **E1** Session `_backend_call_locks` ≠ desktop writer lock | **Still P2.** Keyed by `session_id` at `:80` / `:285`. |
| **E2** `_shares_bot_desktop_browser` false on any `cdp_url` | **LANDED** (round 3). Fence by `features.local`. |

Do not mint a third contract in this PR. Bind Tek’s DISPLAY as a `DisplayTarget` on the #108592 follow-up.

### [Plan share](https://chatgpt.com/share/6aa5d6e5-9f84-83ea-8a58-eb9260568fee) (`6aa5d6e5`)

Two conversations glued.

**Layer A — the only layer that belonged on #108914:** desktop is the resource; generation is the permit; two races + fcntl; “not a redesign.” That gift is **true on this SHA**. The origin comment was exactly that missing `_fence()` before the device op; Tek cherry-picked it.

**Layer B — Kevin kernel / PII / fail-open middleware / vision-freeze / credential handles:** still **do not dump on Tek**. Generation numbers do not cancel in-flight input (D) and do not un-teach a model. That is PER-1558/1566 territory, not 108914.

99% of the competing-PR / OSWorld / “user-space kernel as this PR” tour stays noise.

---

## Latest comments (after `d65af42`)

| Who | Treat as |
| --- | --- |
| **@teknium1** fourth round (`bc36ddb5`, issuecomment-5650098108) | Authoritative map of round 4. Credits kvnloo + smfworks for the fence; cherry-picks 109508 + 109505; jeremiah-k #23 portal one-shot → `display.status` watch; dadhalfdev xkbcomp / ignored release / `--test-type`; BearHuddleston `Notification.show()` **out of this PR** (`electron/notification-ipc.ts`); Fedora receipt closes research-only gap. |
| **@kvnloo** on `d65af42` | KEEP as history. The hole it named is **closed** on this SHA. Do not remint. Do not post “it landed” unless Kevin asks. |
| **@smfworks** #109508 + Arch/pacman receipt | KEEP. Fence is the right patch (now in tree). Pacman: TigerVNC 1.16.2 on `:20`, socket 0600, Hyprland untouched, stop clean; non-fatal gtk/portal noise on a host that already has a seat. |
| **@TheSmokeDev** #109505 | Cherry-picked. Native tests exist (`test_computer_use_native_platform.py`, `windows_only` / `macos_only`). Windows-only CI was still in_progress at fetch. |
| **@coe0718** Fedora 44 receipt | KEEP. dnf5 transaction of `PACKAGES["dnf"]` ok; umbrellas really gone; 8/8 runtime tests with psutil. **Nits (P3):** `tigervnc-server-minimal` is a *Provides* of `tigervnc-x11-server`, not a literal NEVRA; `dbus-run-session` ships from `dbus-daemon`, pulled via `dbus-x11`. Harmless today. |
| Pre-`d65af42` community (helix4u, MrD1az, Julien, …) | Ignore as open. Third-round table + fourth-round fence cover them. |
| @BearHuddleston follow-up | Plasma notification stall: **new issue**, not this PR. Agreed. |

---

## Round-4 bytes (do not remint P1-6)

```303:305:tools/computer_use/tool.py
            _fence()  # input actions never receive fence=; refuse before the device op starts
            result = _dispatch(backend, action, args, fence=_fence)
            _fence()
```

`tests/tools/test_bot_desktop_lease.py::test_takeover_handback_during_approval_does_not_start_the_device_op` patches `_request_approval` (not `_dispatch`), recording backend, asserts `rec.calls == []`. **VERIFIED green.**

`tui_gateway/methods_display_watch.py` `_poll_runtime_files` broadcasts `display.status` when `env` / `launcher.pid` mtime moves. Test: `test_screen_started_or_stopped_by_another_process_is_broadcast_as_status`. jeremiah-k #23 closed.

---

## P2 still open (not merge-blocking)

| ID | Status |
| --- | --- |
| Title `(#92524)` | Still the hosted-browser issue. Body Related is honest. |
| Docs vs default | `bot-screen.md:122` YAML `auto_start: true`; code default `False`. Prose at :67 is honest. `computer-use.md:414` “starts on first use” overstates. |
| `4000 control-taken` “re-attaches” | Header still claims watch-mode re-attach; code sets overlay until manual Reconnect/Take over. |
| Shared-desktop writer lock | `_backend_call_locks` by `session_id`. ChatGPT E1. |
| In-flight input after Take over ack | Tek skip. Uncertain receipt is still a good follow-up (PER-1559). |
| Fedora map nits | Provides vs package name; `dbus-daemon` vs `dbus-x11`. |
| Thumbnail env | Still process-wide `XAUTHORITY` swap, serialized. |
| `display.*` authz | Profile isolation, not peer ACL. Fine for single-user self-host. |
| `ci-reviewed` | Still missing; advisory lockfile (novnc 1.7.0 intentional; bootstrap 0.0.1→0.21.1 is lock drift). |
| #61310 vs `auto_start` | Semantic clash if 61310 hides the tool before dispatch-time start. |

---

## Follow-up PRs targeting this branch

| PR | vs `bc36ddb5` |
| --- | --- |
| **#109508** | Cherry-picked. Now dirty. Close after merge. |
| **#109505** | Cherry-picked. Now dirty. Close after merge. |
| **#109446** | Cherry-picked in round 3. Still dirty (full leftover tree). Close after merge. |
| **#108878** / **#97859** / **#17258** / **#104567** / **#90423** | Unchanged product map. Compose later; don’t dump four desktops. |

Rebase also brought `hermes_cli/profile_channels.py` multiplex from new `main`. That is not Bot Screen. Do not review it as this feature.

---

## Merge checklist (nits, not a second P1 round)

1. Drop `(#92524)` from the title (Related stays).
2. YAML example `auto_start: false` (or omit); soften “starts on first use.”
3. Align 4000 header with overlay-only, or actually re-attach and test.
4. Human `ci-reviewed`. Close dirty follow-up PRs once this lands.
5. Optional: Fedora `Provides` / `dbus-daemon` map; writer lock if two CU sessions share one Bot Desktop.

The control-boundary product is true: generation on disk is used before persist, before vision, before the device op, and while a human holds. That was the ChatGPT Layer A gift. It landed.

Pinned to `bc36ddb5f9696c25acc5d51cf29a961710e2d5a3`.
