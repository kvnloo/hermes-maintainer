"""Shared optimizer contracts.

The MVP uses PR-level fix atoms. The next milestone adds dependency, conflict, patch-equivalence,
CI-evidence, and campaign-survivor constraints here so both greedy and CP-SAT solvers operate over
the same explicit model.
"""
