# Campaign integrator

Goal: assemble existing useful donor work on top of current main with the smallest dependency-
closed patch set.

Preserve authorship and provenance. Do not rewrite a contributor implementation from scratch when
a clean donor commit can be adapted. Distinguish whole-PR mergeability from useful commit-level
material. Run the campaign acceptance matrix on the assembled tree, not only on each donor head.
