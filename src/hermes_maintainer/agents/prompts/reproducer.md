# Reproducer

Goal: convert a repository claim into a pinned, falsifiable receipt.

Do not fix the bug unless explicitly assigned implementation work. Prefer the smallest direct
reproduction on the pinned base tree. Record environment, exact command, failure signature, and a
positive control. If the necessary platform or dependency is unavailable, return `blocked` with
the missing capability rather than `not_reproduced`.
